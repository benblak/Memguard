ProbeRepairDualityKernelV1.lean:253:59: error: unsolved goals
M : ℝ
⊢ ((1 + if probeY = probeX ∨ probeY = repairY then M else 0) + if repairX = probeX ∨ repairX = repairY then M else 0) +
      1 =
    2
Try this:
  [apply] ring_nf
  
  The `ring` tactic failed to close the goal. Use `ring_nf` to obtain a normal form.
    
  Note that `ring` works primarily in *commutative* rings. If you have a noncommutative ring, abelian group or module, consider using `noncomm_ring`, `abel` or `module` instead.
ProbeRepairDualityKernelV1.lean:300:28: error: unsolved goals
M : ℝ
S : Finset Intervention
hp : ProbeOnly S
hc : CoversAll S
⊢ ((1 + M + if repairX = probeX ∨ repairX = probeY then M else 0) +
      if repairY = probeX ∨ repairY = probeY then 1 else 0) =
    1 + M
Try this:
  [apply] ring_nf
  
  The `ring` tactic failed to close the goal. Use `ring_nf` to obtain a normal form.
    
  Note that `ring` works primarily in *commutative* rings. If you have a noncommutative ring, abelian group or module, consider using `noncomm_ring`, `abel` or `module` instead.
ProbeRepairDualityKernelV1.lean:306:28: error: unsolved goals
M : ℝ
S : Finset Intervention
hr : RepairOnly S
hc : CoversAll S
⊢ ((if probeX = repairX ∨ probeX = repairY then 1 else 0) + if probeY = repairX ∨ probeY = repairY then M else 0) = 0
