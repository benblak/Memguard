ProbeRepairDualityKernelV1.lean:250:59: error: unsolved goals
M : ℝ
⊢ (∑ x ∈ {probeX, repairY},
      match x with
      | probeX => 1
      | probeY => M
      | repairX => M
      | repairY => 1) =
    2
Try this:
  [apply] ring_nf
  
  The `ring` tactic failed to close the goal. Use `ring_nf` to obtain a normal form.
    
  Note that `ring` works primarily in *commutative* rings. If you have a noncommutative ring, abelian group or module, consider using `noncomm_ring`, `abel` or `module` instead.
ProbeRepairDualityKernelV1.lean:297:28: error: unsolved goals
M : ℝ
S : Finset Intervention
hp : ProbeOnly S
hc : CoversAll S
⊢ (∑ x ∈ {probeX, probeY},
      match x with
      | probeX => 1
      | probeY => M
      | repairX => M
      | repairY => 1) =
    1 + M
Try this:
  [apply] ring_nf
  
  The `ring` tactic failed to close the goal. Use `ring_nf` to obtain a normal form.
    
  Note that `ring` works primarily in *commutative* rings. If you have a noncommutative ring, abelian group or module, consider using `noncomm_ring`, `abel` or `module` instead.
ProbeRepairDualityKernelV1.lean:303:28: error: unsolved goals
M : ℝ
S : Finset Intervention
hr : RepairOnly S
hc : CoversAll S
⊢ (∑ x ∈ {repairX, repairY},
      match x with
      | probeX => 1
      | probeY => M
      | repairX => M
      | repairY => 1) =
    1 + M
